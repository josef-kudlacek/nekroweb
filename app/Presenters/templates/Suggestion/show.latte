{block content}
    <div class="container">
        <h1 n:block="title">Pravidla hodin</h1>
        {if suggestion}
            <form>
                {foreach $suggestion as $suggestionItem}
                <div class="form-group">
                    <span>{$suggestionItem->Name}</span>
                    <label>{$suggestionItem->Datetime}</label>
                    <label>{$suggestionItem->Text}</label>
                </div>
                {/foreach}
            </form>
        {/if}
    </div>
{/block}